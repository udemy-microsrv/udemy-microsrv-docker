apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: udemy-microsrv-payment
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: udemy-microsrv-payment
            port:
              number: 3000
